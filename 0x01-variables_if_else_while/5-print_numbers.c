#include <stdio.h>
/**
 * main - prints 0 to 9
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int number = 0;
	while (number < 10)
	{
		printf("%d", number);
		number++;
	}
	println("\n");

	return (0);
}


